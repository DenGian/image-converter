# Third-party notices

Runtime dependencies retain their own licences:

- `@imagemagick/magick-wasm` — Apache-2.0; includes ImageMagick and codec notices in the published package.
- `jszip` — MIT and GPL-3.0-or-later dual licence; used under MIT.
- `lucide-react` — ISC; icons are exposed with accessible labels.
- `react` and `react-dom` — MIT.

See `package-lock.json` and each installed package for exact versions and complete notices.
